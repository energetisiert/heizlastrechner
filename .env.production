# Öffentliche Supabase-Werte (geteiltes Projekt "Tool Hub energetisiert.") — kein
# Geheimnis, committet wie bei den Schwester-Tools, damit Vercel sie ohne
# manuelle Dashboard-Konfiguration übernimmt.
#
# Konsolidiert von einem eigenen dedizierten Projekt hierher: Rate-Limiting
# (heizlast_rate_limits / heizlast_bump_rate_limit, umbenannt wegen
# Namenskollision mit dem bestehenden rate_limits im Tool Hub energetisiert.) UND
# die Zugriffskontrolle (Login/Freischaltung/Paket) laufen jetzt über
# denselben Client -- der bisherige zweite NEXT_PUBLIC_HUB_*-Client entfällt.
# Rate-Limiting laeuft ab jetzt ohne Service-Role-Key (anon-EXECUTE auf die
# SECURITY-DEFINER-Funktion, wie bei den anderen Tools) -- SUPABASE_SECRET_KEY
# wird fuer dieses Projekt nicht mehr benoetigt.
NEXT_PUBLIC_SUPABASE_URL=https://gsmuqvjyfjtjbbxqmfgt.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_o38GVg2VcyPJr2pqROPMiQ_neQBsqu1
